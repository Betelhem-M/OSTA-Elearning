const express=require('express');const auth=require('../middleware/authMiddleware');const roles=require('../middleware/roleMiddleware');const c=require('../controllers/eventController');const router=express.Router();
router.get('/',c.getAll);router.get('/my-events',auth,roles('instructor','admin'),c.getMyEvents);router.get('/admin',auth,roles('admin'),c.getAdminEvents);router.get('/:id',c.getById);router.post('/',auth,roles('instructor','admin'),c.create);router.put('/:id',auth,roles('instructor','admin'),c.update);router.patch('/:id/status',auth,roles('instructor','admin'),c.status);router.delete('/:id',auth,roles('instructor','admin'),c.delete);module.exports=router;
