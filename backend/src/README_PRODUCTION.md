# OSTA Backend replacement

Run `config/featureSchema.sql` against the existing OSTA database before using the new verification, password reset, bookmark, and private-question features.

Gmail SMTP environment variables: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, SMTP_FROM. Install nodemailer in the backend package.
