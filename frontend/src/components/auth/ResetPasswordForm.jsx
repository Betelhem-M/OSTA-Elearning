import {useState} from 'react';import {Link} from 'react-router-dom';import {Eye,EyeOff,Mail,ArrowLeft} from 'lucide-react';import api from '@services/api';import Button from '@components/ui/Button';import {validateEmail,validatePassword,validateConfirmPassword} from '@utils/validators';
export default function ResetPasswordForm(){const [step,setStep]=useState('request');const [email,setEmail]=useState('');const [code,setCode]=useState('');const [resetToken,setResetToken]=useState('');const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [show,setShow]=useState(false);const [error,setError]=useState('');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);
async function request(e){e.preventDefault();const er=validateEmail(email);if(er){setError(er);return}setBusy(true);setError('');try{await api.post('/auth/forgot-password',{email});setStep('verify');setMessage('If the account exists, a verification code has been sent to the email address.')}catch(e){setError(e.response?.data?.message||'Could not start password recovery')}finally{setBusy(false)}}
async function verify(e){e.preventDefault();if(!/^\d{6}$/.test(code)){setError('Enter the 6-digit verification code.');return}setBusy(true);setError('');try{const r=await api.post('/auth/forgot-password/verify',{email,code});setResetToken(r.data.resetToken);setStep('reset')}catch(e){setError(e.response?.data?.message||'Invalid or expired code')}finally{setBusy(false)}}
async function reset(e){e.preventDefault();const pe=validatePassword(password),ce=validateConfirmPassword(password,confirm);if(pe||ce){setError(pe||ce);return}setBusy(true);setError('');try{await api.post('/auth/forgot-password/reset',{resetToken,newPassword:password});setStep('done')}catch(e){setError(e.response?.data?.message||'Could not reset password')}finally{setBusy(false)}}
return <div className="mx-auto w-full max-w-[480px] pt-6 sm:pt-10"><section><span className="inline-flex rounded-full bg-primary-light px-3 py-1 text-[10px] font-black uppercase tracking-wide text-primary">Account Recovery</span><h1 className="mt-4 text-2xl font-extrabold text-ink dark:text-white">{step==='request'?'Forgot Your Password?':step==='verify'?'Verify Reset Code':step==='reset'?'Create New Password':'Password Reset Complete'}</h1><p className="mt-2 text-sm leading-6 text-slate-500">{step==='request'?'Enter your email and we will send a secure reset code.':step==='verify'?'Enter the 6-digit code sent to your email.':step==='reset'?'Choose a strong new password for your account.':'Your password has been updated successfully.'}</p>{step==='request'&&<form onSubmit={request} className="mt-8"><label className="mb-2 block text-sm font-bold">Email</label><div className="relative"><Mail size={18} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-400"/><input type="email" value={email} onChange={e=>setEmail(e.target.value)} className="h-12 w-full rounded-lg border bg-white pl-11 pr-4" placeholder="you@example.com"/></div><Button disabled={busy} className="mt-5 h-12 w-full">{busy?'Sending...':'Send Reset Code'}</Button></form>}{step==='verify'&&<form onSubmit={verify} className="mt-8 space-y-4"><input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="h-11 w-full rounded-lg border px-3"/><input value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,''))} maxLength={6} inputMode="numeric" placeholder="6-digit code" className="h-12 w-full rounded-lg border px-3 text-center text-xl tracking-[0.4em]"/><Button disabled={busy} className="h-12 w-full">{busy?'Verifying...':'Verify Code'}</Button></form>}{step==='reset'&&<form onSubmit={reset} className="mt-8 space-y-4"><div className="relative"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password" className="h-12 w-full rounded-lg border px-3 pr-11"/><button type="button" onClick={()=>setShow(v=>!v)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400">{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div><input type={show?'text':'password'} value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm new password" className="h-12 w-full rounded-lg border px-3"/><Button disabled={busy} className="h-12 w-full">{busy?'Updating...':'Reset Password'}</Button></form>}{step==='done'&&<Link to="/login" className="mt-7 flex h-12 items-center justify-center gap-2 rounded-lg bg-primary font-bold text-white"><ArrowLeft size={16}/> Back to Sign In</Link>}{error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-sm font-semibold text-red-700">{error}</p>}{message&&<p className="mt-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{message}</p>}</section></div>}
