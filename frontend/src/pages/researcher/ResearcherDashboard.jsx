import {useEffect,useState} from 'react';import {FileText,CheckCircle,Clock3,Bell} from 'lucide-react';import api from '@services/api';export default function ResearcherDashboard(){const [data,setData]=useState(null);const [error,setError]=useState('');useEffect(()=>{api.get('/portal/researcher').then(r=>setData(r.data?.data)).catch(e=>setError(e.response?.data?.message||'Failed to load dashboard'))},[]);if(error)return <div className="rounded-2xl border border-red-200 bg-red-50 p-6 text-red-700">{error}</div>;if(!data)return <div className="py-12 text-center text-slate-500">Loading researcher dashboard...</div>;const cards=[['Submissions',data.stats.submissions,FileText],['Pending',data.stats.pending,Clock3],['Approved',data.stats.approved,CheckCircle],['Notifications','—',Bell]];return <section><div><h1 className="text-3xl font-black text-ink dark:text-white">Researcher Dashboard</h1><p className="mt-2 text-sm text-slate-500">A live overview of your research activity and submissions.</p></div><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([label,value,Icon])=><div key={label} className="rounded-2xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><Icon className="text-primary" size={20}/><p className="mt-4 text-sm text-slate-500">{label}</p><p className="mt-1 text-2xl font-black text-ink dark:text-white">{value}</p></div>)}</div><div className="mt-8 rounded-2xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><div className="border-b border-slate-100 p-5 font-bold dark:border-slate-800">Recent research activity</div>{data.recent.length===0?<div className="p-8 text-center text-slate-500">No results found.</div>:data.recent.map(x=><div key={x.id} className="flex items-center justify-between border-b border-slate-100 p-5 last:border-0 dark:border-slate-800"><div><p className="font-semibold text-ink dark:text-white">{x.title}</p><p className="mt-1 text-xs text-slate-500">{x.status}</p></div><span className="text-xs text-slate-400">{new Date(x.created_at).toLocaleDateString()}</span></div>)}</div></section>}
