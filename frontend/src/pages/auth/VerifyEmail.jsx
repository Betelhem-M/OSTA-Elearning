import {useState} from 'react';import {useSearchParams,useNavigate,Link} from 'react-router-dom';import api from '@services/api';export default function VerifyEmail(){const [params]=useSearchParams();const [email,setEmail]=useState(params.get('email')||'');const [code,setCode]=useState('');const [message,setMessage]=useState('');const [error,setError]=useState('');const nav=useNavigate();async function verify(e){e.preventDefault();try{await api.post('/auth/verify-email',{email,code});setMessage('Email verified successfully. You can now sign in.');setTimeout(()=>nav('/login'),1000)}catch(e){setError(e.response?.data?.message||'Verification failed')}}async function resend(){try{await api.post('/auth/resend-verification',{email});setMessage('A new verification code has been sent.')}catch(e){setError(e.response?.data?.message||'Could not resend code')}}return <main className="mx-auto flex min-h-[70vh] max-w-md items-center px-5"><div className="w-full rounded-2xl border border-slate-200 bg-white p-7 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h1 className="text-2xl font-black text-ink dark:text-white">Verify your email</h1><p className="mt-2 text-sm text-slate-500">Enter the verification code sent to your email address.</p><form onSubmit={verify} className="mt-6 space-y-4"><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="h-11 w-full rounded-lg border px-3"/><input inputMode="numeric" maxLength={6} value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,''))} placeholder="6-digit code" className="h-12 w-full rounded-lg border px-3 text-center text-xl tracking-[0.4em]"/>{error&&<p className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}{message&&<p className="rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">{message}</p>}<button className="h-11 w-full rounded-lg bg-primary font-bold text-white">Verify email</button></form><button onClick={resend} className="mt-4 w-full text-sm font-bold text-primary">Resend code</button><Link to="/login" className="mt-5 block text-center text-sm text-slate-500">Back to login</Link></div></main>}
